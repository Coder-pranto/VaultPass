const App = ({ children }) => {
  return (
    <div style={{ padding: '20px' }}>
      {/* common UI (navbar / footer later add korte parba) */}
      {children}
    </div>
  );
};

export default App;
